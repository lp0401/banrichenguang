services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: banri-chenguang-web:latest
    container_name: banri-chenguang-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${WEB_PORT:-3000}:3000"
